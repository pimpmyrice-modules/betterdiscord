# yaml-language-server: $schema=../../.json_schemas/module.json

run:
  - action: file
    target: "{{config_dir}}/BetterDiscord/themes/pimp.theme.css"
    template: '{{variant | default("standard")}}.theme.css.j2'
